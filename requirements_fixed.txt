# AutoTrain Advanced - Fixed Requirements
# 修复了 NumPy 2.x 兼容性问题

# 核心依赖
numpy<2.0.0
torch>=2.0.0
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.20.0
peft>=0.7.0
trl>=0.7.0
bitsandbytes>=0.41.0

# 其他依赖
huggingface-hub>=0.19.0
tensorboard>=2.13.0
wandb>=0.15.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy<2.0.0  # 确保 NumPy 版本兼容

# 可选依赖
# unsloth>=2024.1.0  # 如果需要使用 unsloth
# flash-attn>=2.3.0   # 如果需要使用 flash attention 